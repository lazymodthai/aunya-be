export enum DayType {
  WEEKDAY = 'Weekday',
  WEEKEND = 'Weekend',
  HOLIDAY = 'Holiday',
}

export enum BookingStatus {
  CONFIRMED = 'Confirmed',
  PENDING = 'Pending',
  CANCELLED = 'Cancelled',
}

export enum RoomStatus {
  AVAILABLE = 'Available',
  UNAVAILABLE = 'Unavailable',
  MAINTENANCE = 'Maintenance',
}