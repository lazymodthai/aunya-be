import { Controller } from "@nestjs/common";

@Controller("check-slip")
export class CheckSlipController {
  constructor() {}

  // @Post("/check")
  // async PostSlip(@Body() body: CheckSlipDto) {
  //   return this.checkSlipService.SaveSlip(body);
  // }
}
